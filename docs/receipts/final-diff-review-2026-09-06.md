# Semantic V0 最终修复与验证（2026-09-06）

本次合并包含语义分析、CLI/MCP 和本地预览的后续修复。合并代码不代表发布验收通过；当前独立 Gold 和 codegraph 对照回执需要重新评审。

修复发布命令在独立门禁失败时错误返回成功、回执指标和引擎身份校验不足、返回值被重赋值后仍被报告为流向后续调用，以及 trace 输出为 FIFO 时阻塞的问题。预览回归指纹根据源码检查与回归测试刷新，不视为独立 Gold。

本地验证：199/199 测试、类型检查、构建通过；60/60 答案一致性和 54/54 源码检查点通过。Zod、Poetry 与项目自身的选定源码通过 CLI/SQLite/MCP/Markdown 和过期同步检查；内置 demo 与默认 preview 可运行。没有调用外部模型或执行被索引应用。

语法 9 样本 full p95 187.320 ms、增量 p95 79.118 ms、RSS 177808 KiB：临时 300/150 ms、192 MiB 预算通过，原始 150/75 ms 目标未通过。Poetry 192 文件执行 12 轮，每轮 18318 事实，采样进程树峰值 1020985344 字节；每轮启动新的 Pyright 子进程，不证明常驻服务或完整 Python 依赖环境通过。Coverage 保持 partial。

最终发布测量预算通过，但命令正确返回 exit 1：23 个 release 答案和 1 个对照答案发生变化，旧独立回执不再有效。8 个历史上下文不足的问题不计入准确率。后续需独立复评、刷新真实回执并重新执行发布门禁；不得通过重写 passed 字段绕过。

安装依赖后运行 `npm run demo:local` 使用内置 Python 语料。`npm run preview:semantic` 需要本地 Zod 参考源码；参考源码与运行数据库不纳入版本库。

TypeScript grammar 使用锁定的上游提交及兼容补丁；安装脚本生成 ABI 14 parser 并编译原生绑定，需要 Python、C/C++ 编译工具和 make。尚未验收跳过安装脚本或 omit-dev 部署。
