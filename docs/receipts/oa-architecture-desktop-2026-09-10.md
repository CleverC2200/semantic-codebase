# OA 架构图桌面修复与可复现交付

本次将已审阅的桌面 UI 修复保存为显式启用的 renderer 配置和准备入口。Git 基线为 `6d6fd4584b648b84c416249ce400b45152544052`；原工作区的其他修改保留。

## 当前提交验证

| 证据层 | 检查 | 结果 |
|---|---|---|
| source | 补丁与 58 文件摘要清单 | 只改变 template、architecture renderer、共享 CLI、utils 和 i18n 五个文件 |
| local | Node 24.14.1 / 锁文件 `npm ci` | 安装成功，grammar ABI 14 验证通过 |
| local | 定向 renderer 测试 | 5/5，通过真实匹配安装执行；无跳过 |
| local | `npm run typecheck` | 通过 |
| local | 从新准备的 renderer 执行 `deliver` | showcase 9/9，0 error、0 warning |
| local | 重生成结果与已审阅 R3 HTML 比较 | 828102 bytes，逐字节相同 |

HTML SHA-256：`789ee5260da014df9907cbb55de94947227aa10fed94389e15c11dbf4d8eac85`。输入 JSON SHA-256：`65d5d7a15cc8c759d00d684d1971afd6869e9ffe62a5e7bd2df9e7b0c8a2db3d`。配置准备收据与完整交付收据留在本地生成目录，OA 输入、HTML 和截图没有随 PR 发布。

## 复用的浏览器证据

以下验证来自本任务对相同 HTML 哈希的 R3 审阅，本次打包没有重新打分或再做一轮视觉审计：

- 自动浏览器在 1440×900、1600×1000、1920×1080、2048×1320 检查通过。
- 模型检查浅色 1440×900 与深色 2048×1320 截图；原生浏览器验证搜索、来源、指南三入口、图外图例、文字阅读、Escape 焦点恢复和主题切换。
- 普通概览状态中，八个节点副标题的浅色对比度最低约 6.78:1，八个来源徽标最低约 4.98:1。此定向检查不代表全部页面状态都达到无障碍标准。
- 新增文字阅读为 14px；图例与主线路径按钮至少 44px 高。SVG 概览仍存在缩放后的较小文字，提供文字视图作为可读等价入口。

静态检测器原始提示没有逐条裁定，因此没有作为缺陷计数或新评分。上一轮设计 33/40、技术 14/20 属于修复前评估，不能作为本版成绩。完整屏幕阅读器、200% 浏览器缩放、性能基准和全部高级预设/导出仍未覆盖；手机端按用户要求排除。

## 审查范围与边界

准备入口与测试审查目录不覆盖、安装不改写、清单内文件及符号链接边界、失败清理、摘要复现。补丁复用此前 UI 审阅，并逐项检查本次纳入的五文件差异；许可、清单、文档和专用补丁空白属性均在范围内。没有发现阻止此显式桌面配置提交的缺陷。

OA 样本依然是固定历史版本的 8 个模块、8 条关系、8 处来源，输入字节未变。`application_executed=false`；源码展示、模型看图、结构校验和浏览器交互都不等于 OA 运行、审批提交或独立人工签核。该配置没有接管默认 Reader 导出，也没有替换已安装的 Archify。
