# 项目理解与需求核对：本地交付记录

本次在既有冻结阅读器上实现项目总览、中文用途检索、索引／同步／上下文去重三条主线、五条规则的核对与本机保存导出、版本失效提示、紧凑行为步骤，以及源码、Evidence、Fact 的按需加载。#82–#93 的本地实现和自动验证完成；#94 的真人阅读部分尚未完成，不关闭此项。未执行被索引应用，未推送、部署或关闭远端票据。

## 交付身份

- 实现基线：`ba2389c997cb960235f631802368dd9846b84165`。
- 源冻结包：`codegraph-source-reader`，179 文件、2,516 定义、47,858 Fact、146,819 Evidence。
- Snapshot：`546de0ef32493c3b8e9838a0a6163ad398dc98b7452d7c996c3707078b026bac`。
- Overlay：`2420371e34027d16ce18a806eb44f552db3769158083305bafea89c48b62fa26`。
- 原始 reader JSON SHA-256：`946ce55370cf361a6ac257ca7e5079322645a0470a6e6dce6dfd435729ca0da6`。
- 本地输出：`.workspace/acceptance/project-understanding-20260908-release/`；`report.html` 为自包含验收报告，`acceptance.html` 为阅读器，`reader-study.html` 为真人计时与记录表。按需资源目录需随阅读器保留。

使用方式见 [阅读包指南](../guides/project-understanding-reader.md)。默认离线，仅请求当前本地包的内容；完整比较或导出先恢复全部资源。

## 自动化与浏览器证据

- 在排除原有未提交修改的独立 checkout 中，全量测试 **274/274** 通过。此后只补充控制流步骤的中文展示标签，行为定向复验通过；类型检查通过。
- Chrome 8 项旅程通过：首屏无重资源请求、三条主线回源、五规则读取与 Evidence、保存／导出／刷新、搜索进入函数及按需分析、损坏分片保留上下文并重试。
- 当前渲染器可读取旧的完整内嵌包。浏览器完整恢复并导出的 Fact、Evidence、源码与原包逐项相同；三函数的控制流、数据流与调用影响也与原包一致。这是传输和派生结果一致性，不等于原分析绝对正确。
- 独立八函数解释复核复用同一冻结 JSON 的有效记录；本次仅修改 `FileWatcher.flush` 的说明，并重新核对其待处理文件、停止状态和失败次数条件。记录四个源码推导正反例，未执行该函数，不能外推全量准确率。

明细保存在 `.workspace/implementation-20260908/` 的 `full-test.log`、`browser-journey-final.json`、`browser-package-final.json`、`real-assets.json`、`explanation-review.json`。

## 检索与性能

开发者先写预期再执行的十题回归为 10/10。另一评审者独立设计的十题首次严格文件／函数命中为 **5/10**；将有效主线对应源码纳入结果后，保持原预期与口径的回归为 **8/10**。没有事后放宽为“主线出现即算命中”，也不把修复回归称作新的盲测。未命中的两题涉及仅删除文件后的旧缓存处理，以及默认忽略规则与项目配置的组合。

Chrome 152.0.7977.76，同一冻结语料与本机 HTTP 服务，前后交替各五次新进程／临时 profile；不清系统文件缓存。搜索可用时间包含实际输入并出现结果；源码时间包含选择 `FileWatcher.flush` 后源码可读。

| 指标 | 原包 | 新包 |
| --- | ---: | ---: |
| HTML 大小 | 152.23 MB | 约 14.96 MB |
| 搜索可用中位数（范围） | 1,237 ms（1,151–1,340） | 280 ms（248–411） |
| 函数源码可读中位数（范围） | 1,371 ms（1,284–1,498） | 383 ms（359–557） |
| 首屏 JS 堆采样中位数（范围） | 172.4 MiB（约 172.4） | 20.1 MiB（19.8–33.5） |

内存是指定时点的 JS 堆采样，不是进程 RSS 或峰值。新包全部资源约 247.13 MB，因函数分片包含关联 Evidence 而有重复；首屏改善不等于总包体积减少。完整比较／导出仍需全部加载。原始十次记录见 `benchmark-reader-final.json`，保留环境、产物摘要、时间、载荷和内存数据。

## Standards

初审 2 项：旧 Evidence 前缀碰撞阻断历史读取；验证产物导入改变 AI 判断来源。均已修复并通过独立复核。新增有限主线关联检索也已复核，无新增可操作问题。

## Spec

初审 3 项：同 ID 导入覆盖未保存判断；保存合并后未显示冲突；旧 Evidence 前缀碰撞。均已修复，公共 Panel／Loader 回归通过。两轴合计四类独有问题，修复后未解决发现数均为 0。

## 仍需验收

真人参与人数为 **0**。五分钟项目认识、十分钟同步主线解释、误解与定位成本、避免学习影响的对照尚无结果；自动点击和代理检索不能替代。外部 Chrome 的可视走查因 macOS 锁屏未完成；已完成 headless Chrome 自动化及页面截图检查。真实 CodeGraph 文件系统、数据库和业务运行也未执行／验收。Coverage 与业务理解继续保留 partial／unknown。
