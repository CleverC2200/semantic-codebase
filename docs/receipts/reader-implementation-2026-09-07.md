# 源码阅读与变更审阅实现验收

日期：2026-09-07。交付范围：已批准的 #58–73，本地实现、验证、评审和当前分支提交；没有推送、合并或关闭远端 Issue。

## 实际交付

| Issue | 实现与可见边界 |
| --- | --- |
| #58 | 文件页简明函数目录；源码顺序／容器分组；目录与无语义事实定义保留 |
| #59 | 函数关键逻辑；简单流程压缩，分支和未知保留；源码定位 |
| #60 | 直接调用者／当前函数／直接依赖；递归、未知调用和非函数目标身份保留 |
| #61–62 | 系统／业务域／功能候选导航、共享使用与待归类；本机归属确认、撤销、版本历史和需复核状态 |
| #63 | 主线触发、输入输出、阶段与实现；所有阶段说明保持未验证 presentation |
| #64 | 重要性、可逆性和证据状态分开；资金等名称命中仅作候选 |
| #65 | 有预算的反向调用影响；区分位置数和独立调用者，展示路径、截断与未知 |
| #66 | 本地双 Snapshot 阅读包导入／导出；源码哈希与引用校验；显式人工定义对应 |
| #67 | 声明、条件、返回和写入前后差异；写入含完整 Evidence 原文；注释与未知语言保守处理 |
| #68 | 比较、基线／目标函数下钻及返回；版本绑定验证记录与缺口；高调用／敏感候选受控演示 |
| #69 | 参数派生、本地数据路径、跨函数已有映射与写入；保留 file/process 等副作用及未知 |
| #70 | 状态、失败、异步阶段和路径筛选；不猜测事务、补偿和恢复语义 |
| #71 | 有证据的 INHERITS 公共契约影响；数据库、配置和事件依赖仍明确未覆盖 |
| #72 | 指定已有 Execution 与主线对照；版本／哈希失配拒绝；函数观测不升级为内部阶段执行 |
| #73 | 两类离线问题：功能从哪里进入、函数可能影响哪里；消歧、无匹配与不支持反馈 |

没有新增依赖，没有运行被索引应用、调用模型或上传裁剪源码。预览仍使用冻结 Zod 范围和既有 Observation；不是新一次真实运行验收。

## 验证证据

- 本次唯一完整测试：`npm test`，227/227 通过，无跳过。执行于首个实现提交 `c8a0386` 对应代码。
- 评审修复后：`node --import tsx --test test/scripts/semantic-reader*.test.ts`，34/34 通过。
- 随后补充条件内注释反例：比较测试文件 10/10 通过；其余模块无行为修改，复用上条记录。最终新增阅读测试共 35 项。
- 最终 `npm run typecheck`、客户端语法检查、`git diff --check` 通过。
- 当前生成包经 `validateReaderBundle` 回读，435 个定义通过，既有运行观测集哈希与版本匹配。
- `node scripts/refresh-semantic-preview-html.mjs` 只刷新 HTML；没有重建索引或改写旧验收收据。
- 详细命令输出保存在忽略目录 `.workspace/acceptance/reader-implementation/`。

桌面浏览器实测：1280×720。验证文件→函数三步关键逻辑、直接调用图（28 个直接调用者／74 处调用）、两类问题下钻、能力决定写入→刷新回读→撤销、主线运行对照、异步失败路径筛选。双版本导入后人工对应，分别读取 before/after 函数并返回原比较，确认没有混入当前 Zod 的定义。

受控比较夹具位于 `test/fixtures/reader-comparison/`。浏览器已验证 run 的 12 个调用者及返回变化、submitPayment 的资金敏感候选与 `pending→done` 写入变化，以及导入静态投影验证记录后只消除适用缺口。真实执行仍明确未验证。夹具的 `static_possible/controlled_reader_fixture` 不是编译器验收收据。没有执行付款或示例函数。

## 评审与修复

固定评审基点：`4dcfc5bb807ce76c13f2ef6ac4150ed179d61bd3`，首轮目标 `c8a0386`；规范与需求两轴由独立子代理并行检查，修复增量分别回交复核。

- Standards：3 项问题已关闭。非法 Evidence span 可注入 HTML；旧版本／LLM 事实被静态影响消费；导入自报 confirmed 被当作本机确认。修复为整数／范围／版本／来源及引用检查、插值转义、导入确认降级为 imported_unverified 候选。负例独立重放通过。另修正文档 checks 枚举。
- Spec：4 项问题已关闭。写入遗漏右值、file/process 副作用遗漏、导入版本不能下钻、类目标误标函数。修复后增加定向回归；写入注释误报在复核中再次发现并修正，原反例独立重放通过。
- 未报告未修复的阻断项；没有把全量测试通过代替上述边界负例。

覆盖：全部 16 张票的准则已定向审查；首轮 29 个变更路径均有处理结论。Standards 独立审查 26 路径，排除 CSS 视觉与两份解释 JSON 的逐项语义；主任务通过桌面视觉、源码绑定与候选标记核对这些展示范围。后续修复、受控验证夹具和本收据由主任务检查；不宣称逐行语义解释均被独立证明。

## 未验证与使用限制

- 本地静态阅读与受控夹具通过，不等于独立 Gold、任意真实项目、生产业务或真实 Trace 验收。
- 导入阅读包只检查一致性，不重新执行语义准入，也不验证外部提供者身份；所有导入结论保留该边界。
- 能力确认仅针对本机阅读归属，不提升 AI 解释来源，不写 Runtime Capability Registry；浏览器 origin／存储清理会影响记录可见性。
- 尚未覆盖的外部依赖和动态行为始终保持 unknown；调用可达性不代表业务行为改变。
- 按用户范围，本次没有做移动端适配。
