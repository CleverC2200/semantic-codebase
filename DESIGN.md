# 源码阅读工作台

沿用浅色、中性背景与绿色关联强调，服务源码理解和定位。

- 顶部统一搜索文件、函数、项目主线，支持 ⌘/Ctrl K。输入只改变搜索结果，不裁剪目录；窄屏使用独立搜索入口。
- 左侧分项目主线与源码目录。目录保留文件夹、文件、完整函数的稳定层级，采用缩进、层级线和不同标识。
- 当前文件共享文件阅读与函数流程视图，选中函数连续；两种视图分别保留阅读与画布位置。文件阅读按源码顺序排列、按所属定义分组。
- 文件图超过 12 个函数时按源码所属容器折叠；分组可以展开。已解析调用使用实线，未解析目标使用虚线；跨文件出口单独标注，组内调用不冒充函数递归。
- 文件图点击外部函数只打开详情；“进入所在文件”才切换文件。移除通用后退，仅在上下文跳转后提供带目的地的返回入口。
- 图上节点包含名称、简短用途与位置；已有 AI 用途标注来源，其余解释待补齐。连线可查看参数、返回绑定与证据。
- 画布可滚动、拖动、缩放、适应全图、定位选中。聚焦只淡化其余内容，不移除节点。节点几何位置不随详情面板宽度或选中状态变化。
- 项目主线先看阶段、输入输出与分支，点击阶段显示说明、相关函数和源码；按阶段阅读与图双向高亮。候选阶段与说明为 llm_inferred、未验证，并核对源码哈希及原文片段。
- 右侧详情可收起；宽屏支持拖动分隔条或使用左右方向键调整宽度。中等宽度详情替代阅读区，手机详情全宽。图和说明原位置保留。
- 代码使用等宽字体，说明使用系统中文字体；源码和详情独立滚动。完整事实和分析范围按需展开，空状态区分没有函数、内部流程未提取与目标未解析。
- 缩放到全图是结构概览；大量节点时细节通过定位与详情阅读。当前未实现复杂自动布局、绘图编辑器或跨项目总图。
